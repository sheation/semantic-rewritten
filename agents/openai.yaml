interface:
  display_name: "Secprompt Skill"
  short_description: "Use secprompt rewrite and rule workflows"
  default_prompt: "Use $secprompt-skill first to rewrite the request, then pass the rewritten result to the next skill."
